const initialState = {
  defaultArray: [],
};

export const reducers = (state = initialState, { type, payload }) => {
  switch (type) {
    //   case value:

    //   break;

    default:
      break;
  }
};
